import XCTest
@testable import AFFilterList

final class AFFilterListTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AFFilterList().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
